# def test_check_db(client):
#     """Ensure FastAPI is using the SQLite test database."""
#     response = client.get("/api/health")  # ✅ Change to any valid API endpoint
#     assert response.status_code == 200
#     print("✅ Test database is being used correctly!")
